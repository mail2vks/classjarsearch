<project name="common-build" xmlns:ivy="antlib:org.apache.ivy.ant">

	<property name="ivy.install.version" value="2.0.0" />
	<condition property="ivy.home" value="${env.IVY_HOME}">
		<isset property="env.IVY_HOME" />
	</condition>
	<property name="ivy.home" value="${user.home}/.ant" />
	<property name="ivy.jar.dir" value="${ivy.home}/lib" />
	<property name="ivy.jar.file" value="${ivy.jar.dir}/ivy.jar" />
	<property name="ivy.zip.file" value="apache-ivy-2.0.0-bin.zip" />

	<target name="download-ivy" unless="offline">

		<mkdir dir="${ivy.jar.dir}" />
		<!-- download Ivy from web site so that it can be used even without any special installation -->
		<get src="http://www.apache.org/dist/ant/ivy/${ivy.install.version}/${ivy.zip.file}" dest="${ivy.jar.dir}/${ivy.zip.file}" usetimestamp="true" />
		<unzip dest="${ivy.jar.dir}" src="${ivy.jar.dir}/${ivy.zip.file}" />
	</target>

	<target name="init-ivy" depends="download-ivy">
		<!-- try to load ivy here from ivy home, in case the user has not already dropped
              it into ant's lib dir (note that the latter copy will always take precedence).
              We will not fail as long as local lib dir exists (it may be empty) and
              ivy is in at least one of ant's lib dir or the local lib dir. -->
		<path id="ivy.lib.path">
			<fileset dir="${ivy.jar.dir}" includes="**/*.jar" />
		</path>
		<taskdef resource="org/apache/ivy/ant/antlib.xml" uri="antlib:org.apache.ivy.ant" classpathref="ivy.lib.path" />
	</target>

	<target name="resolve" depends="init-ivy">
		<ivy:retrieve />
	</target>

	<target name="compile">
		<mkdir dir="build/classes" />
		<javac classpathref="compile.classpath" srcdir="src" destdir="build/classes" />
	</target>

	<target name="dist" depends="compile" description="Generates Jar File">
		<jar basedir="build/classes" file="build/file.jar">
			<fileset dir="conf">
				<include name="log4j.properties" />
			</fileset>
		</jar>
	</target>

	<target name="all" depends="resolve,compile,dist" />
</project>